'use strict';
const test=require('node:test'); const assert=require('node:assert/strict');
const {toAgentProjection,parseTaskStatus}=require('../../server/harness/agent-compat');
test('projects merged plan and event task fields without secrets or double counting',()=>{const run={id:'run-1',mission:'ship',cwd:'/tmp',status:'awaiting_approval',tasks:[{id:'up-1',title:'Build canonical',status:'completed'}],plan:{tasks:[{id:'up-1',name:'Build planned',description:'d'},{id:'up-2',name:'Test'}]},logs:[],createdAt:'x',useVibes:true,llmPrefs:{provider:'openai',apiKey:'must-not-leak'}};const out=toAgentProjection(run);assert.equal(JSON.stringify(out).includes('must-not-leak'),false);assert.equal(out.totalTasks,2);assert.equal(out.completedTasks,1);assert.equal(out.progress,50);assert.deepEqual(out.tasks[0],{id:'up-1',name:'Build canonical',title:'Build canonical',description:'d',status:'complete'});});
test('compatibility parser preserves upstream task identifiers',()=>{assert.deepEqual(parseTaskStatus('[TASK_STATUS] {"taskId":"up-9","name":"Build","status":"complete"}'),{id:'up-9',taskId:'up-9',name:'Build',status:'complete'});assert.equal(parseTaskStatus('hello'),null);assert.equal(parseTaskStatus('[TASK_STATUS] nope'),null);});
